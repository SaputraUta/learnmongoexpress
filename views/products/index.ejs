<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Products</title>
</head>
<body>
    <ul>
        <% for (let product of products) { %>
            <li>
                <a href="/product/<%= product._id %>"><%= product.name %> - <%= product.brand %></a>
            </li>
        <% } %>
    </ul>
</body>
</html>