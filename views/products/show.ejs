<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= product.name %></title>
</head>
<body>
    <h1><%= product.name %></h1>
    <p>Brand: <%= product.brand %></p>
    <p>Price: <%= product.price %></p>
    <p>Color: <%= product.color %></p>
    <p>Category: <a href="/products?category=<%= product.category %>"><%= product.category %></a></p>
    <a href="/garments/<%= product.garment._id %>">Back to garment</a>
    <a href="/product/<%= product._id %>/edit">Edit</a>
    <form action="/product/<%= product._id %>?_method=DELETE" method="POST"><button type="submit">Delete</button></form>
    <a href="/products">Back to home</a>
</body>
</html>