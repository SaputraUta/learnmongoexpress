<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= garment.name %></title>
  </head>
  <body>
    <h1><%= garment.name %></h1>
    <p>Lokasi: <%= garment.location %></p>
    <p>Kontak: <%= garment.contact %></p>
    <ul>
      <% for (let product of garment.products) { %>
      <li>
        <a href="/product/<%= product._id %>"
          ><%= product.name %> - <%= product.brand %></a
        >
      </li>
      <% } %>
    </ul>
    <a href="/garments/<%= garment._id %>/products/create">Add product</a>
    <a href="/garment/<%= garment._id %>/edit">Edit</a>
    <form action="/garment/<%= garment._id %>?_method=DELETE" method="POST">
      <button type="submit">Delete</button>
    </form>
    <a href="/garments">Back to home</a>
  </body>
</html>
